{
  "private": true,
  "name": "575",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "build:server": "esbuild src/server/index.ts --bundle --outfile=dist/server/index.cjs --format=cjs --platform=node --target=node22 --minify --loader:.json=json",
    "build": "npm run build:server",
    "check": "tsc --noEmit",
    "playtest": "devvit playtest"
  },
  "dependencies": {
    "@devvit/web": "0.14.1",
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "esbuild": "0.23.0",
    "typescript": "5.8.3"
  }
}
